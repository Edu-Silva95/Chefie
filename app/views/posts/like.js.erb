$("#likes-count-<%= @post.id %>").text("<%= @post.reload.likes_count %> Likes");
$("#post-<%= @post.id %> .like-button").replaceWith(
  '<%= j button_to("Unlike", unlike_post_path(@post), method: :delete, remote: true, class: "btn btn-sm btn-outline-primary like-button") %>'
);
